class AppSizes {
  static const double mobileBreakpoint = 800.0;

  // 모바일 패딩
  static const double mobilePadding = 20;
  // 섹션 간 간격 (기존)
  static const double sectionSpacing = 150.0;
  static const double contentTopSpacing = 60.0;

  // Hero 섹션 높이 (기존)
  static const double heroHeight = 700.0;

  // 스크롤 애니메이션 끝점 (기존)
  static const double scrollAnimEnd = 500.0;

  // [새로 추가된 상수들]
  static const double smallSpacing = 20.0;
  static const double mediumSpacing = 40.0;
  static const double largeSpacing = 50.0;
}
